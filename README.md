### Scroll through branches for different examples implementing spring-cloud-function

* master: Demonstrates listening to JSON messages on topic `author-extracted` and splits the messages onto `author-evaluation-passed` & `author-evaluation-passed` topics based on filter criteria.
* avro-processor: Same project as master but avro in and out messages also schema registry.
* simplest-streaming-app: Simplest possible end to end streaming app.


